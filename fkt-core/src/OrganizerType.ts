/**
 * Organizer type, e.g. "person", "store", etc.
 */
 export enum OrganizerType {
	/**
	 * A club.
	 */
	Club = "club",

	/**
	 * A person.
	 */
	Person = "person",

	/**
	 * A store.
	 */
	Store = "store"
}
