name: Build & Test

on: ["push"] # TODO: fix this for pushes and PRs

jobs:
    build-and-test:
        name: Build & Test
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: Build
              run: |
                  yarn install
            - name: Test
              run: |
                  yarn test
